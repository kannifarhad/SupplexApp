import { Express } from "express";
import { createProxyMiddleware } from "http-proxy-middleware";
import constants from "../../constants";
import { decodeJWT, UserError } from "../../utils";

export function applyEndpoints(app: Express) {

}
